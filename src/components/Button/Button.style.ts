import styled from 'styled-components';


export const Button = styled.button`
    display: flex;
    font-size: 18px;
    justify-content: center;
    width: 100%;
    padding: 8px;
    font-weight: bold;
    background: var(--blue);
    color:var(--white);
    border: none;
    border-radius: 5px;
    cursor:pointer;
`;

